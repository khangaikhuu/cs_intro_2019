import java.lang.Math;
public class Function1
{
    private double x;
    private double y;
    public double f(double x, double y)
    {
        return Math.pow(x, 2) + Math.pow(y, 2) - 2 * x * y;
        
    }
}
